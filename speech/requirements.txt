# CUDA wheels. torch cu124 wheels bundle their own CUDA
# + cuDNN, so the runtime base image's CUDA version only needs to satisfy the
# driver (580.x supports CUDA 13, so cu124 runs fine).
--extra-index-url https://download.pytorch.org/whl/cu124
torch==2.6.0
torchaudio==2.6.0

# Episode TTS. Official microsoft/VibeVoice TTS code was removed 2025-09-05;
# this community fork preserves the loader. Pinned commit. The fork itself
# pins transformers==4.51.3 and accelerate==1.6.0 (do not override).
vibevoice @ git+https://github.com/vibevoice-community/VibeVoice.git@07cb79feadd2d3fd7f47530d4c964a12857936a0

# STT + forced alignment (resident).
faster-whisper==1.1.1
# Answer TTS (resident).
kokoro==0.9.4

soundfile==0.13.1
numpy==1.26.4

fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
